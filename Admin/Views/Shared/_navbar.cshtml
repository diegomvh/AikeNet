<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
    <span class="icon-bar"></span>
</a>
<a class="brand" href="#">Cedulas Judiciales</a>
<div class="nav-collapse collapse">
    <ul class="nav">
        <li><a href="@Url.Action("Index", "Home")">Inicio</a></li>
        <li><a href="@Url.Action("Index", "Calle")">Calles</a></li>
    </ul>
</div>
<ul class="nav pull-right">
@if (User.Identity.IsAuthenticated) {
	<li class="divider-vertical"></li>
	<li><a href="#">@{ 
		Admin.Security.User user = ((Admin.Security.User)User);
		<text>
		@user.Organizations[0] 
		</text>
	}</a></li>
    <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <i class="icon-user"></i> @User.Identity.Name <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
        <li><a href="@Url.Action("LogOff", "Account")"> <i class="icon-off"></i> Cerrar sesión </a></li>
    </ul>
    </li>
} else {
    <li>
    <a href="@Url.Action("LogOn", "Account")" class="dropdown-toggle" data-toggle="dropdown">
        <i class="icon-user"></i> Ingresar
    </a>
    </li>
}
</ul>